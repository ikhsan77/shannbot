import type { Canvas as NodeCanvas } from "canvas";
export interface ImageSource {
    width: number;
    height: number;
}
export interface Canvas {
    x: number;
    y: number;
    element: NodeCanvas;
}
export declare const canvasListFromImageData: (image: ImageSource) => Canvas[];
declare const _default: {
    canvasListFromImageData: (image: ImageSource) => Canvas[];
};
export default _default;
